diff --git a/src/colmap/sensor/bitmap.cc b/src/colmap/sensor/bitmap.cc
index c4c4bead..df198bb2 100644
--- a/src/colmap/sensor/bitmap.cc
+++ b/src/colmap/sensor/bitmap.cc
@@ -85,7 +85,7 @@ std::vector<uint8_t> ConvertColorSpace(const uint8_t* src_data,
 void SetImageSpecColorSpace(OIIO::ImageSpec& image_spec,
                             const OIIO::string_view& colorspace) {
   EnsureOpenImageIOInitialized();
-#if OIIO_VERSION >= OIIO_MAKE_VERSION(3, 0, 0)
+#if OIIO_VERSION >= 30000
   image_spec.set_colorspace(colorspace);
 #else
   // Extract logic from 3.0.0 version for backwards compatibility.
@@ -114,7 +114,7 @@ void SetImageSpecColorSpace(OIIO::ImageSpec& image_spec,
 bool IsEquivalentColorSpace(const std::string_view& colorspace1,
                             const std::string_view& colorspace2) {
   EnsureOpenImageIOInitialized();
-#if OIIO_VERSION >= OIIO_MAKE_VERSION(3, 0, 0)
+#if OIIO_VERSION >= 30000
   return OIIO::equivalent_colorspace(colorspace1, colorspace2);
 #else
   // Poor (wo)man's version of available functionality in recent OIIO versions.
diff --git a/src/colmap/util/oiio_utils.cc b/src/colmap/util/oiio_utils.cc
index 120cdafb..bab9bdc9 100644
--- a/src/colmap/util/oiio_utils.cc
+++ b/src/colmap/util/oiio_utils.cc
@@ -12,7 +12,7 @@ void EnsureOpenImageIOInitialized() {
     OIIO::attribute("threads", 1);
     OIIO::attribute("exr_threads", 1);
 
-#if OIIO_VERSION >= OIIO_MAKE_VERSION(2, 5, 3)
+#if OIIO_VERSION >= 20503
     std::atexit([]() { OIIO::shutdown(); });
 #endif
   });
diff --git a/cmake/FindOpenImageIO.cmake b/cmake/FindOpenImageIO.cmake
new file mode 100644
index 00000000..6236afab
--- /dev/null
+++ b/cmake/FindOpenImageIO.cmake
@@ -0,0 +1,31 @@
+# Generated by GLUEMAP's pixi task.
+# Fallback finder for distro OpenImageIO packages without OpenImageIOConfig.cmake.
+
+find_package(PkgConfig QUIET)
+if(PkgConfig_FOUND)
+  pkg_check_modules(PC_OpenImageIO QUIET OpenImageIO)
+endif()
+
+find_path(OpenImageIO_INCLUDE_DIR
+  NAMES OpenImageIO/imageio.h
+  HINTS ${PC_OpenImageIO_INCLUDE_DIRS} ${PC_OpenImageIO_INCLUDEDIR}
+)
+
+find_library(OpenImageIO_LIBRARY
+  NAMES OpenImageIO
+  HINTS ${PC_OpenImageIO_LIBRARY_DIRS} ${PC_OpenImageIO_LIBDIR}
+)
+
+include(FindPackageHandleStandardArgs)
+find_package_handle_standard_args(OpenImageIO
+  REQUIRED_VARS OpenImageIO_LIBRARY OpenImageIO_INCLUDE_DIR
+)
+
+if(OpenImageIO_FOUND AND NOT TARGET OpenImageIO::OpenImageIO)
+  add_library(OpenImageIO::OpenImageIO UNKNOWN IMPORTED)
+  set_target_properties(OpenImageIO::OpenImageIO PROPERTIES
+    IMPORTED_LOCATION "${OpenImageIO_LIBRARY}"
+    INTERFACE_INCLUDE_DIRECTORIES "${OpenImageIO_INCLUDE_DIR}"
+    INTERFACE_COMPILE_OPTIONS "$<$<COMPILE_LANGUAGE:CXX>:-idirafter>;$<$<COMPILE_LANGUAGE:CXX>:${OpenImageIO_INCLUDE_DIR}>"
+  )
+endif()
